{
  "name": "@packages/renderer",
  "version": "0.1.0",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "dev": "echo dev",
    "build": "echo build",
    "typecheck": "npm exec -- tsc -p tsconfig.json --noEmit",
    "test": "echo test"
  },
  "dependencies": {
    "@packages/blocks": "0.1.0"
  }
}
